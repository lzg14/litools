feat: INSERT 解析值展示优化 — JSON 值渲染为可折叠树

- 新增 sqlUnescapeVal：按 MySQL 语义反转义值（'' 还原为单引号，反斜杠转义还原），单/双引号串一致处理，Windows 路径类普通反斜杠不误伤
- JSON 字符串值自动识别：反转义后形似 {/[ 开头即走智能修复，成功渲染为带语法高亮的可折叠小树（复用 jt-toggle 折叠交互，单值 200 节点预算防表格撑爆）；脏 JSON 值同样修复成树
- 纯文本值超过 200 字符折叠显示（前 200 字符 + 总长提示 + ▾，点击展开/收起）
- 列值对照从此可直读：嵌套 JSON 值层层展开，不再是转义字符串一坨
